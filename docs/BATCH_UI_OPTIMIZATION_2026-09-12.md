# 图像处理器弹黑框、画布闪烁：复现与优化评估

日期：2026-09-12。基线版本：0.9.3。状态：已实机复现启动链及文档切换，完成整批输出与单背景导出可行性验证；**尚未修改正式桥接、安装文件或发布包**。

## 测试范围与保护

- 使用用户指定“测试”文件夹，当时共有 24 张 JPEG。
- 使用已有动作“自建动作 / 绿色上衣SAM抠图调色-脚本”，先只读检查全部 15 步：兼容桥、选择并遮住、复制图层及调整层；未发现保存/关闭源文件步骤。
- 调用实际安装的 Adobe `Image Processor.jsx`。诊断包装器仅在内存中设置截图所需参数：包括子文件夹、保留结构、JPEG 质量 12、无缩放、运行上述动作。保留原对话框，实际点击“运行”，执行 Adobe 原有处理流程。
- 输出定向到全新 `work/batch-focus-1789188001668`；包装器不回写用户的图像处理器默认设置，不改 Adobe 安装文件。
- 24 张输出文件名与输入逐一一致，JPEG 完整性验证 24/24 通过，总计 170,477,842 字节。源文件前后 SHA-256 全部一致。
- `work/batch-focus-observation.txt`：12:40:01 打开测试流程，12:44:26 结束。记录的 264,390 ms **包含对话框停留时间**，不是净处理耗时，也不是模型推理耗时。
- 使用电脑操作技能观察真实 UI 和窗口标题；没有操作终端 UI、修改系统安全设置或控制用户其他应用。

## 1. 黑色命令窗口

实机捕获的父子进程及命令行为：

```text
Photoshop.exe
  -> cmd.exe /c
     -> cmd.exe /d /s /c
        -> python.exe .../sam31_backend/legacy_bridge.py
           -> 已启动/复用的 SAM 本地服务
```

`legacy/SAM31 Image Processor Bridge.jsx` 每张图都执行 `app.system(command)`，其中 command 又显式包装 `cmd.exe`。实际宿主添加了外层 `cmd /c`。这与用户截图的 cmd 黑窗口对应，不是 SAM 的正常进度面板，也不是置信度提示。

`legacy_bridge.py` 启动模型服务时已有 `CREATE_NO_WINDOW`；UXP 原生 supervisor 也已有该标志。只修改最里面的模型进程无法消除前面的 shell。正常复用模型时仍要启动每张图的桥接客户端，所以仍可能每张弹窗。

### 优先级 P0：替换 shell 启动链

建议提供一个不创建 UI 的 Windows 子系统桥接启动器/客户端，避免经 `app.system` 启动命令解释器；直接用无控制台方式启动所需 Python 服务或桥接进程。不能只在原命令上加 `start /min`、换成 `pythonw`，也不能仍通过 `app.system` 启动新启动器。

Windows 的 `CREATE_NO_WINDOW` 可以让控制台子进程不创建控制台；`/SUBSYSTEM:WINDOWS` 的程序本身不要求控制台。这是方案的技术依据，不代表本轮已完成整条启动链改造。[Microsoft 进程创建标志](https://learn.microsoft.com/en-us/windows/win32/procthread/process-creation-flags)、[Windows 子系统选项](https://learn.microsoft.com/en-us/cpp/build/reference/subsystem-specify-subsystem?view=msvc-170)。

实施要求：

1. 明确的请求文件交接、请求 ID 校验、结果原子写入和等待完成语义，不能启动后立刻让后续动作执行。
2. 只允许固定运行时与结构化推理请求，不接受任意 shell 命令。
3. 保持按需启动、空闲退出、一次自动重试、技术错误报错、NO_OBJECT 全画布后备。
4. 保留旧脚本事件 ID 与动作参数兼容，现有动作原则上无需重录。
5. 不为了减少闪烁而改回图像处理器同步调用普通 UXP 动作；旧调度器阻塞问题仍需规避。
6. 新客户端须随后端安装器正式部署，版本升级后回归，不能只依赖开发机存在某个工具。

## 2. 画布闪烁

对真实批处理进行窗口标题采样，实际观察到以下阶段：

```text
原照片
  -> FR-SAM-export 临时文档
  -> 原照片：mask / FR SAM temporary mask 临时图层
  -> 原照片：复制图层、调整层
  -> Adobe Photoshop 2026 空文档窗口
  -> 下一张照片
```

源码解释：

- 输入导出复制整个文档，然后复制活动层、隐藏其他图层、导出 PNG、关闭临时文档。这造成额外文档切换。
- 掩码置入后要校正位置和尺寸，当前在这些操作之后才设置 `visible=false`。函数名虽为“隐藏置入”，但不能据此保证置入过程从未可见。
- 图像处理器自己的逐张打开/关闭，以及用户动作中的“选择并遮住”、调整层操作，也有宿主重绘。不能把所有闪动归于一个原因，也不能承诺整个 Photoshop 工作流零闪烁。

### 优先级 P1：单背景图层安全快路径

在用户文件夹一张 5027×5027、单背景图层、无 ROI 的真实图片上运行 `tests/photoshop/probe-background-export.jsx`：

| 路径 | 单次观测导出耗时 | 是否创建临时文档 |
|---|---:|---|
| 当前安装桥接的 `saveInputPlanes` | 1347 ms | 是 |
| 当前文档直接另存 PNG 副本（asCopy=true） | 578 ms | 否 |

解码后 RGBA 字节逐一相同，ICC 相同。两者 RGBA SHA-256 都是 `039a6fef6c1ec9e20c9d8f343d3c6ac4d192c091c0127aa4b8d6f8387c73f9e1`。文档名称、已保存状态、历史状态、活动层、文档数量检查通过，源照片未保存改动。

这是一个样本的可行性证据，不是通用性能保证。正式加入时应仅在严格的单背景、RGB8、无 ROI 等已验证条件下启用；多图层、智能对象、透明度、样式和非标准活动通道等继续使用可靠路径，不能改变“只读取当前活动图层”的约定。需要更多样本、中文路径及异常恢复测试。

### 优先级 P2：临时掩码的不可见导入

先验证在隐藏容器内置入掩码、再载入其透明度的方案；至少应避免等到全部变换完成后才隐藏。必须比较最终灰度选区、全画布坐标、严格 ROI、原活动层/通道恢复及失败回滚，不能只看画面不闪就验收。

## 推荐执行顺序与验收

1. 首先替换 shell 启动链，目标是正常成功路径不出现命令窗口、不因桥接客户端抢走其他应用焦点。
2. 然后加入单背景输入快路径，减少每张额外文档切换。
3. 最后优化掩码置入可见性，区分桥接闪烁与图像处理器自身打开/关闭的正常切换。
4. 在同一 24 张文件夹、同一动作、同一输出设置下回归；比较输出尺寸、名称、选区语义、无对象继续、技术错误、后台运行和原文件哈希。

以上为首次诊断结论；该次只新增诊断脚本和记录，没有修改正式 0.9.3 行为。后续执行结果如下。

## 2026-09-12 执行结果

用户要求先提交推送、再执行优化。已先将诊断、运行时恢复记录及选择模式需求提交为 `d475469`，推送到 `origin/main`，再实施下列改动。

### 已实现并部署

1. **P0 无窗口启动**：新增 `fr-sam-legacy-launcher.exe`，Windows GUI 子系统、静态 C++ 运行库。ExtendScript 通过 `File.execute()` 直接启动，不再调用 `app.system`、`cmd`、PowerShell 或 VBS。启动器只处理当前用户固定临时根目录中的新鲜、固定命名请求，原子认领后以 `CreateProcessW / CREATE_NO_WINDOW` 启动已安装的 Python 桥接客户端。参数只来自经过校验的运行时配置及固定任务文件，不接收任意命令。
2. **同步完成语义**：请求先完整写入，再放置 ready 标记；启动器原子发布完成标记，脚本等待完成后才读取原子响应并继续后续动作。保留 requestId、掩码路径和响应类型校验。启动接收超时 30 秒；最长任务等待约 25 分钟，覆盖原有两次 10 分钟 HTTP 超时及冷启动预算，不是日常推理预期耗时。保留后端一次重启重试、空闲退出和 NO_OBJECT 全画布后备。
3. **P1 安全快速导出**：只对单背景层、RGB8、正常混合、100% 不透明度/填充、正常通道、无蒙版/样式/ROI 的文档直接保存 PNG 副本。智能对象、多图层、透明层、额外通道、ROI 等仍走原隔离导出，不改变活动层读取规则。
4. **P2 隐藏掩码**：先建立隐藏容器与内部锚点，再置入掩码，立即校验父容器处于隐藏状态。保留完整画布变换校正、透明度载入、异常历史回滚和临时层清理。
5. **打包路径**：CMake 构建新启动器；Prepare-PublicRelease 将其放入 `payload/bin` 并运行原生测试；Inno 安装器部署至 `{app}/bin`。本轮已成功生成并检查暂存构建。没有覆盖 GitHub 既有 0.9.3 资产，也没有发布新版本。

### 发现并处理的旧脚本残留

仅更新当前用户脚本目录并不足以修复本机旧动作：实测该动作调用的是 Photoshop `Program Files/Presets/Scripts/SAM31 Image Processor Bridge.jsx`。第一次整批复验仍观察到 `cmd.exe` 和 `FR-SAM-export`，因此主动停止，该次输出不作为优化验收。

随后通过 Windows 正常管理员提升运行带备份的维护更新，实际替换受保护的旧脚本；没有改 ACL、关闭安全软件或修改系统安全设置。用户目录的正式入口、旧兼容入口及 Program Files 旧兼容入口均已同步；`frSamImageProcessorBridge` / `sam31ImageProcessorBridge` 两个事件 ID 保留，用户原动作无需重录。维护脚本为 `installer/tools/Deploy-BatchBridgeUpdate.ps1`，它先检查全部目标写入权限，再备份和更新。不要将此维护脚本录入 Photoshop 动作。

### 实测验收

| 项目 | 结果 |
|---|---|
| Node / 安装修复测试 | 47 项通过 |
| Python 后端单元测试 | 15 项通过，包括严格 ROI、软 Alpha、启动失败重试一次 |
| 原生测试 | 运行时配置、静默启动器两套通过；覆盖中文路径、无控制台子进程、失败、取消和完成标记 |
| 安装器构建 | Inno Setup 编译成功，压缩清单包含 bin/fr-sam-legacy-launcher.exe；仅生成独立 QA 包，未重装或发布此包 |
| PNG 输入等价性 | 同一 5027×5027 照片，RGBA 与 ICC 逐字节相同；文档状态不变 |
| 单张导出耗时 | 同轮旧路径 1255 ms，新路径 609 ms；仅为单样本观测 |
| 隐藏掩码等价性 | 72/300 DPI 均与旧路径逐像素相同；244 个 Alpha 等级；置入刚完成时已处于隐藏父容器中 |
| 实际图像处理器 + 原用户动作 | 完整 24/24 输出，无需重录动作 |
| 输出对比 | 24 张名称、尺寸及解码后 RGB 全部与旧版相同；合计 170477842 字节 |
| 原图保护 | 测试前后 24 张 SHA-256 全部相同 |
| 进程采样 | 24 次原生静默启动器、25 个 Python 进程；本次桥接链未观察到 cmd.exe |
| 文档切换采样 | 本次单背景批次未出现 FR-SAM-export 临时文档标题 |
| 最终安装版功能 | 注册脚本事件正常选中、NO_OBJECT 全选并继续复制图层、正式入口 NO_OBJECT、智能对象 + 成功识别严格 ROI 均通过；无临时图层/通道泄漏 |

本机证据位于忽略目录（不随源码上传）：

- `work/batch-ui-QA-1789189436741`：旧/新输入及选区 PNG 对比。
- `work/batch-focus-1789190203492`：最终 24 张图像处理器输出；对比基线 `work/batch-focus-1789188001668`。
- `work/batch-ui-process-observation.json`：最终进程及标题采样；`work/batch-ui-process-old-entry.json`：旧入口反例。
- `work/runtime-recovery-regression.txt`：最终普通层、智能对象、ROI、全选后备回归。
- `work/bridge-update-backup-1789190164183`：更新前脚本及启动器备份。
- `work/installer-batch-ui-QA/FR-SAM-Backend-batch-ui-unreleased.exe`：仅用于打包验收的未发布安装器，不替代既有公开包。

`observe-image-processor.jsx` 的计时包含等待点击“运行”的时间，不能用其 161532 ms 直接宣称净批处理性能或提升比例。后续若要量化吞吐，需要统一热身和自动起止点。

### 仍保留的边界

- 图像处理器自身打开/关闭图片、“选择并遮住”、调整层等仍会重绘；不承诺整个 Photoshop 零闪烁或所有宿主窗口不激活。此次针对桥接创建的黑框和额外文档/掩码显示。
- 复杂图层与 ROI 仍需临时隔离文档，因此仍可能有额外切换；不能为减少重绘而读取合成图像。
- Photoshop 2025、Windows 10、16 GB 显卡以及 1000 张真实整套动作仍未在本机补做实机验收。
- 双选择模式仍独立记录于 `SELECTION_MODES_REQUIREMENTS.md`，本轮没有夹带实现或更改单个最佳对象语义。
- 本轮是已部署的源码维护修复，面板版本仍显示 0.9.3；公开发布下一版前须统一递增版本、打包并复核，不替换既有发布资产。
