# FR SAM 文本选区：架构与执行计划

> 版本：0.9.3 Pre-release
>
> 更新日期：2026-09-12
>
> 目标：Windows 10/11、Photoshop 2025/2026、NVIDIA GPU（最低 16 GB）

新增已确认需求（待实现，不属于现有 v0.9.3 能力）：[单个最佳／全部匹配模式及动作兼容规则](SELECTION_MODES_REQUIREMENTS.md)。2026-09-12 当前用户运行时配置恢复见 [修复与实机验证记录](RUNTIME_RECOVERY_2026-09-12.md)。

图像处理器的命令窗口与画布闪烁已完成 24 张实图诊断及单背景导出对照，优化方案尚未实施，见 [批处理界面优化评估](BATCH_UI_OPTIMIZATION_2026-09-12.md)。

## 1. 产品结论

项目采用 Adobe Manifest v6 Hybrid UXP 插件加当前用户级 Windows 后端。Photoshop 负责活动图层捕获、动作参数和选区写回；本地后端负责模型生命周期与 SAM 3.1 推理。后端不依赖 ComfyUI 或用户 Python，首次请求按需启动，批次内复用，空闲后退出。

用户侧保留两个入口：

1. 交互面板：修改英文提示词和置信度后生成选区，运行中显示取消按钮。
2. 动作入口：把提示词、阈值、模型 ID 和参数版本保存在 Photoshop 动作步骤中。常规动作、Batch 和 PSJS 使用 UXP 动作；原生图像处理器使用安装器部署的 ExtendScript 兼容动作。

## 2. 冻结范围

| 项目 | v0.9.3 决策 |
|---|---|
| 输入 | 当前活动图层，普通像素层和智能对象 |
| 图层语义 | 读取该层自身像素与透明度，忽略下方合成 |
| 文档模式 | RGB 8 位；其他模式直接报错 |
| 原选区 | 作为严格搜索 ROI；匹配成功时输出不得越界，无对象后备例外 |
| 提示词 | 仅英文，支持短语及逗号分隔候选，最多 5 个 |
| 候选 | 所有提示词和候选中全局置信度 Top-1 |
| 阈值 | 默认 0.50，范围 0.05–0.95，随动作保存 |
| 输出 | 一次性替换为 0–255 灰度软选区 |
| 无对象 | 明确 NO_OBJECT 时全选整个画布并继续，不重试，覆盖旧 ROI |
| 技术故障 | 自动重启并重试同一请求一次，仍失败则抛错 |
| 后处理 | 全部交给后续 Photoshop 动作 |
| 日志 | 本地轮转，最多 3 个；不记录提示词、图像、源路径或令牌 |
| 分发 | 免费 CCX 加小型联网 Windows 安装器 |

## 3. 架构

Photoshop Hybrid UXP 插件包含中文面板、Action Recorder、短时捕获模态、模态外推理、短时提交模态，以及最小 x64 原生 Addon。Addon 只管理后端进程、回环认证、健康检查、一次重启重试和空闲退出，不在 Photoshop 进程中加载 CUDA。

后端只绑定 127.0.0.1。每次会话使用随机 256-bit 令牌，图像经受限临时会话目录传输。Validator 拒绝目录逃逸、超过 96 MP、非 RGB8、非法提示词和不匹配模型。Meta 适配器使用官方 SAM 3.1 源码提交 660a5e9e1b8b4c02c0ad97229b88a09a6e4ff5b7，选择全局 Top-1 概率掩码，并在 CPU 侧按活动层透明度和 ROI 严格合成。

图像处理器是宿主兼容特例：Photoshop 2026 的旧 ExtendScript 调度器同步调用纯 UXP 动作时会阻塞首个 Imaging/DOM 操作。因此安装器为 PS 2025/2026 的当前用户脚本目录部署兼容桥。2026-09-12 维护优化将外部启动改为无窗口原生程序；安全的单背景图层直接导出 PNG 副本，复杂输入仍用隔离文档；掩码从置入时即处于隐藏容器中，载入选区后删除临时内容。详见 BATCH_UI_OPTIMIZATION_2026-09-12.md。

## 4. 性能和资源

- 最大文档：8000×12000（96 MP）。
- 发布最低显存：16 GB；安装前使用 NVIDIA SMI 预检。
- RTX 5090 目标：4000×6000 热端到端不超过 8 秒；冷启动不计。
- 本轮实测：最终 CCX 的后端首次推理约 9.80 秒；最终安装版后端连续处理 20 张真实大图为 20/20 成功、总计 69.16 秒，热请求约 1.73–2.07 秒。另有 AlphaProxy 1000 次 HTTP 请求通过，不等同于真实图片端到端压力验收。
- 推理采用低分辨率检测/精炼和 CPU 全尺寸掩码合成，避免 96 MP 全分辨率 CUDA 掩码。

## 5. 安装和发布

Windows 安装器以当前用户权限安装到 LocalAppData。安装时选择引用已有固定哈希模型，或从 Comfy-Org 的固定地址下载并校验。它安装 CPython 3.10.11 embedded、PyTorch 2.10.0+cu128、torchvision 0.25.0+cu128、锁定依赖、官方 SAM 源码和图像处理器桥，随后强制离线运行预检。

CCX 使用正式 ID com.fangrui.sam-selection、名称 FR SAM 文本选区、版本 0.9.3、Photoshop 最低版本 26.0.0。发布资产附带 SHA256SUMS.txt、MIT 许可证、Meta SAM 许可证和第三方通知。当前预发布包未做商业代码签名。每次修复增加版本号，发布前核验 CCX、暂存及实际安装文件一致；不原地覆盖已公开的版本包。

## 6. 验证状态

以下为 v0.9.2 历史基线。v0.9.3 详细执行步骤见 EXECUTION_PLAN_v0.9.3.md，本轮证据见 TEST_REPORT_v0.9.3.md。

已通过：

- Python 单元测试 15/15。
- 插件 Node 测试 21/21，另有纯逻辑套件通过。
- Release 原生 Addon 测试 2/2，包括正式运行时启动。
- 独立固定运行时离线预检及真实模型冷/热推理。
- 最终安装版后端对测试文件夹 20 张真实图片的连续模型推理，20/20 成功。
- 1000 次连续本地 HTTP 请求（AlphaProxy 传输测试）。
- 本轮 Photoshop 2026 27.10 普通层、智能对象、严格 ROI、软选区和面板运行状态。历史动作、Batch、PSJS 验收与当前复测范围分别记录，详见 TEST_REPORT_v0.9.2.md。
- Windows 安装器当前用户安装、模型复用、稳定运行时、自检和卸载数据策略。

预发布限制：

- 当前机器没有 Photoshop 2025、Windows 10 或恰好 16 GB 的 NVIDIA GPU，不能伪造这些同配置实机结果。
- Batch 遇到真正的技术/输入错误仍可能显示宿主确认；0.9.3 的 NO_OBJECT 改为全选后继续，不触发该错误路径。后续动作如何处理整图由用户录制的命令决定。
- SAM 3.1 概率掩码能形成软选区，但不是专用 alpha matting，毛发、薄纱和半透明质量取决于模型能力。
- 包未签名，因此用 Pre-release 收集安装与兼容反馈。

## 7. v1.0 后续

根据预发布反馈补齐 Photoshop 2025、Windows 10、16 GB 显卡矩阵；评估代码签名、安装失败码强化及第二个本地精细抠图模型。第二模型若引入，必须保持当前动作参数可迁移、离线运行和后端可替换边界。

## 8. v0.9.2 无人值守修复计划

- 已完成：核对配置文件、注册表、源码、CCX 与实际安装目录，定位缺失权限及同版本安装残留；新增自动发布核验。
- 已完成：修复 UXP 面板参数反馈、重入、取消状态和输出区域布局；原选区读取失败时停止，提交前验证文档/图层/历史状态。
- 已完成：修复后端启动重试、在途请求空闲保护、64 位父进程句柄、HTTP 超时和非致命日志；原生工作线程由插件持有并在退出时收尾。
- 已完成：运行时先暂存校验再切换，配置原子替换；修复 PowerShell 5.1 配置替换参数及安装失败退出码。
- 已完成：修复 JSX 构建标记的宿主编译错误，新增 Photoshop 真实编译器检查脚本。
- 桥接专项：先判断再解除剪贴；通过临时 Alpha 通道传递复制文档时丢失的原选区，恢复源文档状态后再推理。掩码按完整画布校正坐标，避免原选区/视口居中造成偏移，导入失败回滚。增加连续成功、真正无对象错误及临时文档/图层/通道清理验收。
- 发布门禁：最终安装包、CCX、安装文件一致性、真实图像及已安装桥接回归已通过，新进程面板取消与重试成功。按 v0.9.2 提交、推送及发布 Pre-release；完整结果见测试报告，未实测矩阵和长时间混合自动化调试后的宿主等待明确保留为预发布限制。

## 9. v0.9.3 用户确认的增量

两个入口统一实行 NO_OBJECT 全选后备，不增加跳过分支或额外确认；仅本次请求的明确错误码可进入后备。扩大停靠/浮动默认高度，短窗口保持可滚动，底部留白。保持插件 ID、动作 handler 与参数 schema 不变。详见 EXECUTION_PLAN_v0.9.3.md 的步骤、边界和验收记录。

## 10. 2026-09-12 桥接维护优化

已在基线 `d475469` 推送后实施无窗口启动、安全直接导出、隐藏容器掩码三项优化。最终本机原图像处理器动作 24/24 通过，输出像素与旧版 24/24 相同，原图哈希未变；桥接链采样无 cmd，普通背景批次无 FR-SAM-export。受保护的旧动作脚本已通过正常管理员提升、备份后更新。普通层、智能对象、严格 ROI 与 NO_OBJECT 全画布继续均复验通过。

本轮不发布新 GitHub 资产；源码维护修复已部署，面板仍显示 0.9.3。下一次公开发布递增版本并按发布门禁打包。双选择模式仍待实施，不属于此次闪烁优化。
